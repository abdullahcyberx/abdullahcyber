import fs from 'fs'; const main = fs.readFileSync('src/main.js', 'utf8'); const newAi = fs.readFileSync('C:/Users/Dell/.gemini/antigravity/brain/49f4ef3d-ada6-43a4-aae9-3ff26782a368/scratch/new-ai.js', 'utf8'); const idx = main.indexOf('  /* -------------------------\r\n     Shehzada\'s AI'); const realIdx = idx !== -1 ? idx : main.indexOf('  /* -------------------------\n     Shehzada\'s AI'); if (realIdx !== -1) { fs.writeFileSync('src/main.js', main.substring(0, realIdx) + newAi); console.log('Replaced successfully'); } else { console.log('Marker not found'); }
